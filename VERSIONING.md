# 版本、审校、提交与 Release 规范

本规范是项目版本管理、翻译审校、Git 提交和公开发布的统一规则，适用于开发仓库、公开
发布仓库以及项目后续建立的其他仓库。构建、验证、生成产物、修改文档或更新发布仓库，
都不会自动授权 Git 提交、推送或创建 Release。

> 生效说明：本规范建立前已经正式发布的版本保持原样，不为了统一格式而回改历史；本规
> 范从下一个开发周期开始完整执行。

## 1. 两个仓库的职责边界

项目分为开发仓库和玩家发布仓库，两者不是同一个项目副本，不能混用：

| 仓库 | 职责 | 禁止事项 |
| --- | --- | --- |
| `D:\Tools\TR2013Mod` | 唯一的翻译、审校、权威源、构建、测试、审计和维护文档仓库 | 不得把发布仓库或 GitHub 自动生成的源码包作为构建输入；未经明确授权默认不 push |
| `D:\5_game_mod_github\TR2013-Simplified-Chinese` | 面向玩家下载的 README、许可、第三方声明、公开图片和 GitHub Release | 不保存权威译文、review、构建脚本、原文快照或内部维护记录；不得反向覆盖开发仓库 |

最终 ZIP 由开发仓库的最终验证 commit 构建，只作为对应 GitHub Release 的手动附件；不把
ZIP 提交进发布仓库 Git 历史。发布仓库不提交 `.gitignore`，本机临时文件如需防误提交，
只使用该工作区自己的 `.git/info/exclude`。

## 2. 版本开发周期

一个版本允许包含多个 commit。版本号表示这一组提交共同归属的开发周期，不要求每个
commit 都增加版本号。

例如，最新正式 Release 为 `v1.2` 时，后续开发进入 `v1.3`，在 `v1.3` 正式发布前可以
依次存在：

```text
v1.3：更新译文
v1.3：修正文档
v1.3：调整构建基线
v1.3：完成发布前验证
```

这些 commit 都属于同一个 `v1.3` 开发周期。只有 `v1.3` 正式发布以后，后续修改才进入
`v1.4`。不得因为同一版本中增加了第二个、第三个 commit，就提前切换到下一版本。

版本递增遵循当前项目的一位小数版本规则：

```text
v1.2 -> v1.3
v1.3 -> v1.4
...
v1.9 -> v2.0
```

## 3. 翻译审校与建议框闸门

翻译纠正必须先进入开发仓库 `review/` 的“修改建议”和“理由”区域，不得先直接改写
权威译文。建议框是作者的决策队列，不是自动应用队列：

1. 审校以英文原文为首要依据，结合文本类型、剧情场景、人物身份与语气、系列术语、
   玩家理解和资源技术安全逐条判断。
2. 只有明确改进才填写建议；建议必须是完整替换文本并附理由，控制码、时间、占位符、
   格式和角括号边界必须安全保留。
3. 审校可以分批进行，批次大小按质量和效率自行决定；不能为了赶进度机械翻译、刻意
   缩短、过度润色、添加原文没有的信息或改变人物意图。证据不足时保留原译，建议框
   留空。
4. 非空建议框表示“待作者审阅”，不表示已经批准。作者可以接受、修改或拒绝每条建议。
5. 只有作者明确表示可以应用后，才能执行 `review_suggestions.py apply --confirm`，将
   通过检查的建议写入 UI、SCH 或字幕质量权威源并记录历史。
6. 应用建议后必须同步更新受影响的项目文档，重新完成相关的完整构建、独立验证、文本
   与资源审计和发布产物检查；不能复用应用前的旧 ZIP。
7. 最终 ZIP 验证通过后，才能执行 `review_suggestions.py refresh --confirm` 重新导出
   review。刷新后的对照表必须反映当前最新 Mod，已处理的建议框必须全部为空；仍有待
   决建议时不得用普通导出覆盖审校记录。

详细命令、权威来源、控制码规则和失败保护见 [`REVIEW-WORKFLOW.md`](REVIEW-WORKFLOW.md)。

## 4. commit 与 Release 的对应关系

每个正式 Release 必须对应其版本开发周期中最新且完整验证通过的 commit：

```text
v1.3 commit
    |
v1.3 commit
    |
v1.3 最终验证 commit  <-- v1.3 tag / v1.3 Release
```

发布时必须满足：

1. 该版本计划包含的修改已经全部完成。
2. 对应仓库工作区没有遗漏的未提交修改。
3. 自动测试、完整构建、独立验证和发布产物检查全部通过。
4. 发布 ZIP 确实由准备打标签的开发仓库最终 commit 构建。
5. 发布仓库的 Release 标签精确指向发布仓库最新且已推送的同版本 commit。
6. Release 附件的文件名、大小、内容和 SHA-256 与开发仓库最终产物一致。

不得把标签指向同一版本的较早 commit，也不得在正式发布后继续追加属于该旧版本的
commit；发布后的修正必须进入递增后的新版本。

## 5. 正式 Release 冻结

Release 一旦正式发布，其内容立即冻结，包括：

- Git tag 及其指向的 commit
- Release 标题、正文和更新摘要
- 已上传的 ZIP、安装包及其他附件
- 附件文件名、大小和内容

正式发布后不得编辑 Release 说明，不得替换、删除或重新上传附件，也不得移动或重建同名
标签。如果发现译文、文档、安装说明、Release 说明或附件存在问题，应当保留旧 Release
原样，将修正纳入下一个版本，并使用递增后的版本号创建新的 commit、tag 和 Release。

Release 草稿在正式发布前可以检查和修正；草稿一旦发布，就适用上述冻结规则。

## 6. README、Nexus 详情页与 Release 摘要边界

README 只保存长期有效的项目介绍、功能、安装、更新、卸载、兼容性、故障排查、许可和
反馈渠道，不保存“当前版本”、`Version Notes / 更新摘要`、某个版本的变更列表或某个
Release 专属的验证结果和附件校验值。每个版本的更新说明只写入对应 Release。

发布仓库的长期许可调整必须同步检查并更新 `LICENSE.md`、`README.md` 和
`THIRD_PARTY_NOTICES.md`。完整原创内容规则以 `LICENSE.md` 为准，README 只提供玩家向摘要；
第三方内容继续以其适用的官方许可证为准。许可变更属于长期文档变更，不得只修改某一处
后留下相互矛盾的说明。

项目原创 Mod 内容可以按照 `LICENSE.md` 规定免费、非商业地搬运、转载、镜像和分发原始完整包；
原始、未修改的 Mod 文件可以纳入免费、非商业整合包或安装器。传播者必须保留作者、项目名称、许可证、
来源链接、第三方声明、非官方说明和原始文件完整性。项目原创 Mod 的修改版、替换版、删减版和衍生发布版
不得再分发；GitHub Fork 可以存在，但不授权发布修改后的 Mod。出售、收费下载、付费访问以及会员、广告、
赞助或捐赠门槛等变相收费均禁止。第三方字体继续以 OFL 1.1 为准，不受上述原创 Mod 限制。

正式 Release 发布后，许可正文、README 中对应的长期说明和第三方声明均视为冻结。若需再次
调整传播范围或权利条件，应进入下一开发版本，并在新的 Release 中说明；不得回改已发布
Release 的历史内容。

每个版本的 Release 摘要必须：

- 面向下载 Mod 的玩家，用清楚的可见变化说明本版本做了什么；
- 只描述本版本新增或修正的内容，不把 v7.5 或更早版本的工作归入当前版本；
- 不用笼统的“全面优化”“完善项目”代替实际变化；
- 不重复 README 中的安装要求，内部脚本名、审校统计、构建过程和测试数字只有在确实
  有玩家价值时才可写入；
- 在发布前一次性核对，正式发布后不再修改。

开发仓库的 `Nexus-description.txt` 是面向玩家的详情页，已有正文、历史摘要和 BBCode
排版属于固定格式：

- 不改写或重新排版已固定的旧版本摘要；
- 版本标题统一使用现有格式 `[size=4][b]vX.Y[/b][/size]`，不得把当前版本标题写成不
  加粗格式；
- 除用户明确要求外，不擅自新增或删除 `[b]`、`[size]`、`[color]`、列表和换行等 BBCode；
- 当前版本摘要只追加当前版本的实际改动，不能把项目长期已有能力冒充为本版本新增。

## 7. commit、push 与发布必须分别授权

以下三项是彼此独立的操作，必须分别获得作者明确授权：

1. 创建 commit；
2. push 到远端仓库；
3. 创建并正式发布 Release。

“修改”“修复”“更新”“构建”“验证”“生成 ZIP”“更新发布项目”等指令，只授权完成对应
工作，不自动包含 commit、push 或发布授权。执行规则如下：

- 没有明确要求提交 commit 时，修改只能保留在本地工作区；
- 开发仓库默认不 push，必须得到针对开发仓库的明确 push 授权；
- 发布仓库必须在创建 Release 前完成明确授权的 commit 和 push，Release 不能指向未推送
  的本地 commit；
- 没有明确要求发布 Release 时，即使已有 commit、push 和 ZIP，也不得创建 Release；
- 一次授权不延伸到后续版本、另一个仓库或下一项操作。

## 8. 提交信息

同一开发版本内的所有提交信息都使用该版本前缀：

```text
v<开发版本>：<本次提交摘要>
```

例如最新正式 Release 为 `v1.2`、当前开发版本为 `v1.3` 时：

```text
v1.3：修正 README Markdown 格式
```

不得继续使用已经正式发布并冻结的旧版本前缀。开发仓库和发布仓库的提交都遵循当前
开发版本前缀；同一版本的多个 commit 不切换版本号。

## 9. 标准发布顺序

1. 确认最新正式 Release，按规则将开发版本递增 `0.1`。
2. 在开发仓库 review 建议框中完成审校；作者批准后应用建议并更新相关文档。
3. 从变更涉及的步骤开始执行完整构建、自动测试、独立验证、资源审计和最终 ZIP 检查。
4. 确认开发仓库工作区完整，提交该版本一个或多个 commit；如作者要求合并，须在最终
   验证和发布前完成合并，仍使用同一版本前缀。
5. 确认最终 ZIP 确实来自开发仓库最新且已验证的该版本 commit。
6. 将长期有效的 README、许可、第三方说明或公开图片变更同步到发布仓库；版本摘要只
   写入对应 Release，不把开发维护日志复制给玩家。
7. 分别获得两个仓库的 commit 授权后提交；开发仓库默认保持不 push。
8. 获得发布仓库 push 授权后推送发布仓库最终 commit；需要推送开发仓库时另行取得授权。
9. 获得 Release 授权后，为已推送的发布仓库最终 commit 创建同版本 tag 和 Release，上传
   已验证 ZIP，并写入只属于该版本的玩家向摘要。
10. 从 GitHub 端回读 tag、目标 commit、正文、附件名、大小和 SHA-256，确认与本地记录一致。
11. Release 正式发布后冻结；任何新修改都进入下一版本开发周期。

## 10. 完成工作时的状态报告

每次结束都必须明确报告：

- 修改了哪些文件，属于哪个仓库；
- 是否已暂存；
- 是否已创建 commit、commit ID 和提交信息；
- 是否已 push，push 到哪个仓库和分支；
- 是否已创建或更新 Release、tag、附件及校验结果；
- 哪些授权尚未获得，因此哪些操作没有执行。
